name: Inputs demo
on: 
  workflow_call:  # some one should call
    inputs:
        project:
         description: 'Project name'
         required: true
         type: string
        component:
          description: 'component name'
          required: true
          type: string
        environment:
          description: 'Env name'
          required: true
          type: string
jobs:
  # Job name
  trigger:
    runs-on: ubuntu-latest # Github hosted runners
    steps:
      - name: checkout code
        uses: actions/checkout@v6

      - name: list the files 
        run: ls -l  
         
             